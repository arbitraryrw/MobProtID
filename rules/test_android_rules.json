{
    "rules": [
      {
        "ruleId":"001",
        "ruleName": "R2 function parsing check",
        "description" : "POC IN PROGRESS",
        "condition": "OR",
        "part_1": {
            "type": "functions",
            "handler": "radare2",
            "matchType": "regex",
            "matchValue": [
              "(?i)(.*)"
            ]
        }
      }
    ]
  }
