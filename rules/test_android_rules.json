{
    "rules": [
      {
        "ruleId":"001",
        "ruleName": "yara",
        "description" : "POC IN PROGRESS",
        "condition": "OR",
        "part_1": {
            "type": "unknown",
            "handler": "yara",
            "matchType": "regex",
            "matchValue": [
              "(?i)(.*)"
            ]
        }
      }

    ]
  }
